/**
 * Default dashboard/API URL baked into the extension so a public install
 * from the Chrome Web Store works without the user ever needing to know
 * infrastructure details. Overridable via the Options page's Advanced
 * section for self-hosting/dev.
 */
export const DEFAULT_API_BASE_URL = "https://recall-dashboard-five.vercel.app";
